---
apiVersion: v1
kind: Service
metadata:
  name: portal-app-stable
  namespace: portal-app
spec:
  selector:
    app: portal-app
  ports:
    - port: 80
      targetPort: 3000

---
apiVersion: v1
kind: Service
metadata:
  name: portal-app-canary
  namespace: portal-app
spec:
  selector:
    app: portal-app
  ports:
    - port: 80
      targetPort: 3000
